# Fourth-conjugation verbs

```{julia}
#| warning: false
#| output: false
#| echo: false


repo = pwd() |> dirname |> dirname

using Tabulae, CitableParserBuilder
using  Markdown

shareddir = joinpath(repo, "datasets", "core-infl-shared") 
lat25dir = joinpath(repo, "datasets", "core-infl-lat25") 
ds = dataset([shareddir, lat25dir])
```

`ds` is a core data set:


```{julia}
#| warning: false
#| asis: true
audio = LexemeUrn("ls.n4453")
md_verb_conjugation(audio,ds) |> Markdown.parse
```